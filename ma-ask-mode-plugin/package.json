{
  "name": "ma-ask-mode-plugin",
  "private": true,
  "version": "0.3.0",
  "description": "External user plugin for minimal-agent: read-only Ask mode (harness refuses Edit/Write, model presents diffs).",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "bun run ../scripts/typecheck.ts",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "biome:check": "biome check .",
    "biome:fix": "biome check --write .",
    "check": "bun run ../scripts/check.ts"
  }
}
