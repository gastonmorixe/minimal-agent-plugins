{
  "name": "ma-computer-use-plugin",
  "private": true,
  "version": "0.1.0",
  "description": "External user plugin for minimal-agent: full control of the Mac via a signed Swift helper (Accessibility inspect/interact + CGEvent mouse/keyboard + ScreenCaptureKit), driven over a unix socket. Tool: Computer.",
  "type": "module",
  "scripts": {
    "test": "bun test --pass-with-no-tests",
    "typecheck": "bun run ../scripts/typecheck.ts",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "biome:check": "biome check .",
    "biome:fix": "biome check --write .",
    "check": "bun run ../scripts/check.ts",
    "cud": "bun run bin/cud.ts",
    "native:build": "bun run bin/cud.ts build"
  }
}
