{
  "name": "minimal-agent-plugins",
  "private": true,
  "version": "0.0.0",
  "description": "External user plugins for minimal-agent. Each subdirectory is one plugin package.",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "format": "biome format --write .",
    "check": "bun run typecheck && bun test"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
