{
  "id": "config",
  "name": "Config",
  "version": "0.1.0",
  "description": "Interactive editor for ~/.minimal-agent/config.jsonc. The /config command opens a footer overlay to browse and edit settings (model, effort, thinkingDisplay, mode, formatter, status bar, etc.) and toggle every discovered plugin on/off. Edits are comment-preserving: your // notes survive a save. Also /config get <id> and /config path for headless use.",
  "commands": [
    {
      "name": "config",
      "summary": "Edit minimal-agent settings interactively",
      "argHint": "[get <id> | path]",
      "handler": { "type": "module", "path": "./handlers/cmd_config.ts", "export": "default" }
    }
  ],
  "hooks": [
    {
      "id": "on_key",
      "channel": "editor.key",
      "handler": { "type": "module", "path": "./handlers/on_key.ts", "export": "default" },
      "priority": 80
    }
  ],
  "permissions": [
    "hooks:editor.key",
    "hooks:editor.footer.set",
    "hooks:editor.overlay.open",
    "hooks:editor.overlay.close"
  ]
}
