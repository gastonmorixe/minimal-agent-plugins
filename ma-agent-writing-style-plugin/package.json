{
  "name": "ma-agent-writing-style-plugin",
  "private": true,
  "version": "0.1.0",
  "description": "External user plugin for minimal-agent: pure-prompt fragment enforcing agent writing style.",
  "type": "module",
  "scripts": {
    "test": "echo 'ma-agent-writing-style-plugin: pure-prompt plugin, no tests'",
    "typecheck": "bun run ../scripts/typecheck.ts",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "biome:check": "biome check .",
    "biome:fix": "biome check --write .",
    "check": "bun run ../scripts/check.ts"
  }
}
