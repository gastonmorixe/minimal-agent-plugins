{
  "name": "ma-model-info-plugin",
  "private": true,
  "version": "0.1.0",
  "description": "Contributes the ModelInfo tool: a decoupled, provider-agnostic way for the model to read its own current capabilities and session details at call time.",
  "type": "module",
  "scripts": {
    "test": "bun test --pass-with-no-tests",
    "typecheck": "bun run ../scripts/typecheck.ts",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "biome:check": "biome check .",
    "biome:fix": "biome check --write .",
    "check": "bun run ../scripts/check.ts"
  }
}
