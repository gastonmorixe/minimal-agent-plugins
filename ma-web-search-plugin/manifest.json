{
  "id": "web-search",
  "name": "Web Search",
  "version": "0.1.0",
  "description": "Search the web from the agent. Provider-pluggable, ships with Brave as the default.",
  "tuis": [
    {
      "id": "web_search",
      "trigger": {
        "type": "tool",
        "tool": {
          "name": "WebSearch",
          "description": "Search the web for current information. Returns ranked hits with title, URL, snippet, age, and source. Use type='news' for time-sensitive queries (recent events, breaking news). Default 'web' for general lookups, documentation, and reference material. Use format='json' only when you need to extract specific fields programmatically. The default 'text' is more compact and easier to read inline.",
          "input_schema": {
            "type": "object",
            "properties": {
              "query": {
                "type": "string",
                "description": "The search query. Max 400 characters. Brevity helps, most providers do better with 2-6 well-chosen keywords than full sentences."
              },
              "type": {
                "type": "string",
                "enum": ["web", "news"],
                "default": "web",
                "description": "Search vertical. 'web' = general results, 'news' = time-sensitive recent articles."
              },
              "count": {
                "type": "integer",
                "minimum": 1,
                "maximum": 20,
                "default": 10,
                "description": "How many hits to return. Default 10. Smaller is usually better, top 5 are typically the only ones worth reading."
              },
              "offset": {
                "type": "integer",
                "minimum": 0,
                "description": "Page index (0-based). Use to paginate beyond the first page."
              },
              "freshness": {
                "type": "string",
                "description": "Time filter: 'pd' (past day), 'pw' (past week), 'pm' (past month), 'py' (past year), or 'YYYY-MM-DDtoYYYY-MM-DD' for a custom range."
              },
              "country": {
                "type": "string",
                "description": "ISO-2 country code (e.g. 'US', 'DE') or 'ALL'. Affects result region."
              },
              "lang": {
                "type": "string",
                "description": "2-character language code (e.g. 'en', 'de'). Affects result language."
              },
              "safesearch": {
                "type": "string",
                "enum": ["off", "moderate", "strict"],
                "description": "Adult content filter."
              },
              "format": {
                "type": "string",
                "enum": ["text", "json"],
                "default": "text",
                "description": "Result format. 'text' (default) returns a compact human-readable list. 'json' returns a structured shape with the same fields, suitable when you need to extract specific values."
              }
            },
            "required": ["query"],
            "additionalProperties": false
          },
          "explicitName": true,
          "reason": "perfect collision: plugin IS the tool name"
        }
      },
      "handler": {
        "type": "module",
        "path": "./handlers/web_search.ts",
        "export": "default"
      },
      "interactive": false,
      "icon": "󰖟",
      "color": "gold"
    }
  ]
}
